# PostgreSQL connection string used by Prisma
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ordersdb?schema=public"

# HTTP server
PORT=3000

# Worker tuning (milliseconds)
WORKER_POLL_INTERVAL_MS=1000
WORKER_PROCESSING_DELAY_MS=2000
WORKER_BATCH_SIZE=5
WORKER_MAX_ATTEMPTS=3
